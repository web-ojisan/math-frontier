# CLAUDE.md

このファイルは本リポジトリで Claude Code(および開発者)が従うべき方針をまとめたものです。
実装前に必ず読み、疑問点は着手前に確認してください。

## サービス概要

子ども(まず6歳〜小学生、将来的に中学生まで)が、2のべき乗や平方数などの数列を
**「暗算で伸ばす」→「暗記して即答する」の2段階**で身につけていく、遊びだけのアプリ。

- **項目=遷移**: 「8192→16384」のような「前項から次項」を1項目とする。
  フロンティア(列の端を暗算で伸ばす)も高速想起(内側を即答で引く)も同じ出題形式で、
  違いは反応時間に表れる。
- **3状態モデル**: 項目ごとに「未到達(unknown)/暗算で出せる(mental)/自動化済み(auto)」を
  反応時間ログから推定する。遊んでいるだけで状態遷移が記録される。
- **推定は変動・表示は単調**: 状態推定は実力の現在値に追従して上下する(降級あり)。
  子どもに見せる数字(自己ベスト、どこまで伸びたか)は下がらない値だけにする。
- **ゲーム演出**: スマホゲー風のターン制ボス戦。**答えた数そのものがダメージ**
  (2^14=16384を答えれば16384ダメージ)。自動化済み判定の即答なら1.5倍+派手なエフェクト。
  誤答は「攻撃が外れる」程度の軽いペナルティ。連続ミスでターン終了(連打対策)。
  ボスHPはその子のフロンティアから逆算して「ちょっと頑張れば倒せる」に調整する。
- **バトルの出題は完全固定の昇順**(列の最初から登る。2→4→8→…)。ランダムに前の項へ
  飛ばない(ユーザー決定 2026-09-03)。壁(2連続ミス)に当たったら最初から登り直し。
  ボスHPは「1回の登り切り+フロンティアへの一歩」で倒せる規模に逆算する。
  ランダム出題の高速想起(scheduler.ts)は将来の「そくとうモード」で使う。
- **比較は自分の過去だけ**: 他人との比較・ランキングは出さない(自己ベスト、先週の自分)。
- **親の協力プレイ**(将来): 同じ端末で交代して同じボスを攻撃。競争ではなく協力。
  親の反応時間も同形式でログに残す。親不在でも一人で成立すること。
- ガチャ・課金要素は入れない。キャラクター・画面デザインは既存ゲームに寄せず完全オリジナル
  (ターン制・ボス戦・引っ張って攻撃などのゲームの型は使ってよい)。

### スコープ外(今は作らない。ただしログ形式だけ将来を考慮)

親向けサマリ(週次ダイジェスト)、課金、パズル類、逆引き出題(「289は何の平方?」等)。
逆引きはデータモデル上の考慮のみ行う。

## 技術スタック(合意済み)

- **PWA**(まずはネイティブにしない)。**子どもの端末はiPad中心**(スマホは持たせない方針。
  持たせるとしてもノートPC)。よって:
  - レイアウトはタブレットの画面サイズ・横持ちを一級市民として設計する(スマホ縦画面は二の次)
  - **物理キーボード入力(数字キー+Enterで攻撃)にも対応**する(ノートPC・iPad+キーボード)
  - iOS Safariのローカルデータ消失リスクに配慮する(記録のエクスポート機能を早期に検討)
  - 振動APIは使えない前提の演出にする。ストア配布が必要になったらCapacitorで包む。
- **Vite + TypeScript + React** のクライアントのみSPA。**バックエンドなし・認証なし**。
- 記録は端末ローカル(IndexedDB)。項目ごとの試行ログ(項目、答え、正誤、反応時間、時刻)を残す。

## 計測・推定の規約

- **反応時間の主信号は「出題表示から最初のキー入力までの時間(firstKeyMs)」**。
  回答確定までの時間(totalMs)は補助・記録用。多桁入力(16384は5桁)で
  「桁数が多い=遅い」と誤判定しないための決定。
- 状態遷移のルール(閾値・連続回数・降級条件)は src/state-estimator.ts に集約し、
  シミュレーションテストで妥当性を担保する。

## 題材(数列)の規約

- 問題は**すべてプログラム生成**。市販教材を入力にしない。**LLMは使わない**。
- 数列は「列の定義(次の項の求め方、表示形式、逆引きの有無)」をデータとして
  src/sequences.ts に持たせ、追加しやすくする。

## コーディング規約

- コメントとエラーメッセージは**日本語**で書く。
- 「今は使わない」抽象化・共通化は行わない。重複が3箇所目に出てから共通化を検討する。
- コアロジック(数列定義・状態推定・出題選択)は**純粋関数**で書き、UIから分離してテストする。
- 6歳が一人で操作できるUIを最優先(読める字が少ない前提、数字入力はテンキー、
  1セッションは数分で終わる)。

## 進め方に関する方針

- 大きな仕様変更・新しいマイルストーンに着手する前は、計画と疑問点を提示し、
  ユーザーの承認を得てから実装する。各ステップで設計判断を短く説明してから実装する。
- 小さく動くものを優先する。
